document.addEventListener("DOMContentLoaded", () => {
	const status = document.getElementById("status");
	if (status) {
		status.textContent = "Open Instagram and click \"Show Post\" on any post to see its image and caption.";
	}
});
